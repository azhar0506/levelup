# Disk Usage Logging Script

This script runs `df -h` to check disk usage and logs the results everytime it runs.
It keeps normal output and errors **seperated** and **appends** to the logs instead of overwriting them. 

